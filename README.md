# Reyhoon Vue Full Stack


### Install dependencies

```sh
npm run setup
```

### Setup up .env file
1. Create environment file from the `backend` folder by copying the `.env.example` file and add respective values.

### To run the project in development

```sh
npm start
```
